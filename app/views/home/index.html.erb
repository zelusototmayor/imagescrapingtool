<div class="min-h-screen bg-gradient-to-br from-slate-50 via-blue-50 to-indigo-50">
  <!-- Navigation Header -->
  <nav class="bg-white/80 backdrop-blur-md border-b border-white/20 sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center space-x-3">
          <div class="w-8 h-8 bg-gradient-to-r from-blue-600 to-indigo-600 rounded-lg flex items-center justify-center">
            <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
          </div>
          <span class="text-xl font-bold bg-gradient-to-r from-blue-600 to-indigo-600 bg-clip-text text-transparent">ImageSweep</span>
        </div>
        <div class="hidden md:flex items-center space-x-6 text-sm text-gray-600">
          <a href="/demo" class="hover:text-blue-600 transition-colors">Demo</a>
          <a href="#" class="hover:text-blue-600 transition-colors">Documentation</a>
          <a href="#" class="hover:text-blue-600 transition-colors">API</a>
          <a href="#" class="hover:text-blue-600 transition-colors">Support</a>
        </div>
      </div>
    </div>
  </nav>

  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <!-- Hero Section -->
    <div class="text-center mb-12">
      <div class="inline-flex items-center px-4 py-2 rounded-full bg-blue-50 border border-blue-200 text-blue-700 text-sm font-medium mb-6">
        <svg class="w-4 h-4 mr-2" fill="currentColor" viewBox="0 0 20 20">
          <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd"></path>
        </svg>
        Fast & Efficient Image Scraping
      </div>
      
      <h1 class="text-5xl md:text-6xl font-bold text-gray-900 mb-6 leading-tight">
        Scrape Images from
        <span class="bg-gradient-to-r from-blue-600 via-purple-600 to-indigo-600 bg-clip-text text-transparent">Any Website</span>
      </h1>
      
      <p class="text-xl text-gray-600 max-w-2xl mx-auto leading-relaxed">
        Extract and download all images from any website with our powerful, intelligent scraping engine. 
        Perfect for designers, developers, and content creators.
      </p>
    </div>

    <!-- Main Form Card -->
    <div class="bg-white/70 backdrop-blur-sm rounded-2xl shadow-2xl border border-white/20 p-8 mb-8">
      <form id="scrapeForm" class="space-y-8">
        <!-- URL Input Section -->
        <div class="space-y-4">
          <label for="url" class="block text-lg font-semibold text-gray-800 mb-3">
            <div class="flex items-center space-x-2">
              <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"></path>
              </svg>
              <span>Website URL</span>
            </div>
          </label>
          <div class="relative">
            <input 
              type="url" 
              id="url" 
              name="url" 
              placeholder="https://example.com" 
              required
              class="w-full px-6 py-4 border-2 border-gray-200 rounded-xl focus:ring-4 focus:ring-blue-100 focus:border-blue-500 text-lg transition-all duration-200 bg-white/80 backdrop-blur-sm"
            >
            <div class="absolute inset-y-0 right-0 pr-3 flex items-center pointer-events-none">
              <svg class="h-5 w-5 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
              </svg>
            </div>
          </div>
        </div>

        <!-- Advanced Options -->
        <div class="space-y-6">
          <details class="group">
            <summary class="cursor-pointer flex items-center justify-between p-4 rounded-xl bg-gradient-to-r from-gray-50 to-blue-50 border border-gray-200 hover:border-blue-300 transition-all duration-200">
              <div class="flex items-center space-x-3">
                <svg class="w-5 h-5 text-blue-600 group-open:rotate-90 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"></path>
                </svg>
                <span class="text-lg font-semibold text-gray-800">Advanced Options</span>
              </div>
              <svg class="w-5 h-5 text-gray-500 group-open:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"></path>
              </svg>
            </summary>
            <div class="mt-4 space-y-6 p-6 bg-white/60 rounded-xl border border-gray-200">
              <!-- Render JavaScript -->
              <div class="flex items-center space-x-4 p-4 bg-blue-50 rounded-lg border border-blue-200">
                <input 
                  type="checkbox" 
                  id="render_js" 
                  name="render_js" 
                  checked
                  class="w-5 h-5 text-blue-600 border-gray-300 rounded-lg focus:ring-3 focus:ring-blue-200 focus:ring-offset-0"
                >
                <div class="flex-1">
                  <label for="render_js" class="text-sm font-semibold text-gray-800">Render JavaScript</label>
                  <p class="text-sm text-gray-600">Recommended for modern sites with dynamic content</p>
                </div>
              </div>

              <!-- Scrape Mode -->
              <div class="space-y-4">
                <label class="block text-sm font-semibold text-gray-800 mb-3">Scraping Mode</label>
                
                <!-- Current Page Only -->
                <div class="flex items-center space-x-4 p-4 bg-blue-50 rounded-lg border border-blue-200">
                  <input 
                    type="radio" 
                    id="scrape_mode_current" 
                    name="scrape_mode" 
                    value="current_page"
                    checked
                    class="w-5 h-5 text-blue-600 border-gray-300 focus:ring-3 focus:ring-blue-200 focus:ring-offset-0"
                  >
                  <div class="flex-1">
                    <label for="scrape_mode_current" class="text-sm font-semibold text-gray-800">Current Page Only</label>
                    <p class="text-sm text-gray-600">Just scrape this specific page</p>
                  </div>
                </div>

                <!-- Same Section -->
                <div class="flex items-center space-x-4 p-4 bg-green-50 rounded-lg border border-green-200">
                  <input 
                    type="radio" 
                    id="scrape_mode_subpath" 
                    name="scrape_mode" 
                    value="subpath_only"
                    class="w-5 h-5 text-green-600 border-gray-300 focus:ring-3 focus:ring-green-200 focus:ring-offset-0"
                  >
                  <div class="flex-1">
                    <label for="scrape_mode_subpath" class="text-sm font-semibold text-gray-800">Same Section</label>
                    <p class="text-sm text-gray-600">Stay within this section/category of the website</p>
                  </div>
                </div>

                <!-- Entire Website -->
                <div class="flex items-center space-x-4 p-4 bg-purple-50 rounded-lg border border-purple-200">
                  <input 
                    type="radio" 
                    id="scrape_mode_entire" 
                    name="scrape_mode" 
                    value="entire_website"
                    class="w-5 h-5 text-purple-600 border-gray-300 focus:ring-3 focus:ring-purple-200 focus:ring-offset-0"
                  >
                  <div class="flex-1">
                    <label for="scrape_mode_entire" class="text-sm font-semibold text-gray-800">Entire Website</label>
                    <p class="text-sm text-gray-600">Crawl the whole website starting from this page</p>
                  </div>
                </div>
              </div>
            </div>
          </details>
        </div>

        <!-- Submit Button -->
        <button 
          type="submit" 
          class="w-full bg-gradient-to-r from-blue-600 to-indigo-600 text-white py-5 px-8 rounded-xl hover:from-blue-700 hover:to-indigo-700 focus:ring-4 focus:ring-blue-200 font-semibold text-lg transition-all duration-200 transform hover:scale-[1.02] disabled:opacity-50 disabled:cursor-not-allowed disabled:transform-none shadow-lg hover:shadow-xl"
          id="submitButton"
        >
          <div class="flex items-center justify-center space-x-3">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
            <span>Start Scraping Images</span>
          </div>
        </button>
      </form>
    </div>

    <!-- Job Status Card (hidden initially) -->
    <div id="statusCard" class="bg-white/70 backdrop-blur-sm rounded-2xl shadow-2xl border border-white/20 p-8 mb-8 hidden">
      <div class="flex items-center space-x-3 mb-6">
        <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
          <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900">Scraping in Progress</h2>
      </div>
      
      <!-- Progress Section -->
      <div class="space-y-6">
        <!-- Progress Bar -->
        <div class="space-y-3">
          <div class="flex justify-between items-center">
            <span id="statusText" class="text-lg font-semibold text-gray-700">Queued</span>
            <span id="progressText" class="text-lg font-bold text-blue-600">0%</span>
          </div>
          <div class="w-full bg-gray-200 rounded-full h-3 overflow-hidden">
            <div id="progressBar" class="bg-gradient-to-r from-blue-500 to-indigo-500 h-3 rounded-full transition-all duration-500 ease-out" style="width: 0%"></div>
          </div>
        </div>

        <!-- Status Message -->
        <div class="p-4 bg-blue-50 rounded-xl border border-blue-200">
          <p id="statusMessage" class="text-blue-800 font-medium"></p>
        </div>

        <!-- Stats Grid -->
        <div class="grid grid-cols-2 gap-6">
          <div class="text-center p-6 bg-gradient-to-br from-blue-50 to-blue-100 rounded-xl border border-blue-200">
            <div id="pageCount" class="text-3xl font-bold text-blue-600 mb-2">0</div>
            <div class="text-sm font-medium text-blue-700">Pages Crawled</div>
          </div>
          <div class="text-center p-6 bg-gradient-to-br from-green-50 to-green-100 rounded-xl border border-green-200">
            <div id="imageCount" class="text-3xl font-bold text-green-600 mb-2">0</div>
            <div class="text-sm font-medium text-green-700">Images Found</div>
          </div>
        </div>
      </div>
    </div>

    <!-- Results Card (hidden initially) -->
    <div id="resultsCard" class="bg-white/70 backdrop-blur-sm rounded-2xl shadow-2xl border border-white/20 p-8 mb-8 hidden">
      <div class="flex items-center space-x-3 mb-6">
        <div class="w-10 h-10 bg-green-100 rounded-full flex items-center justify-center">
          <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
        </div>
        <h2 class="text-2xl font-bold text-gray-900">Scraping Complete!</h2>
      </div>
      
      <!-- Download Section -->
      <div class="space-y-4 mb-8">
        <h3 class="text-lg font-semibold text-gray-800 mb-4">Download Your Images</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <button 
            id="downloadZip" 
            class="flex items-center justify-center space-x-3 w-full bg-gradient-to-r from-green-600 to-emerald-600 text-white py-4 px-6 rounded-xl hover:from-green-700 hover:to-emerald-700 focus:ring-4 focus:ring-green-200 font-semibold transition-all duration-200 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            <span>Download ZIP Archive</span>
          </button>
          <button 
            id="downloadManifest" 
            class="flex items-center justify-center space-x-3 w-full bg-gradient-to-r from-gray-600 to-slate-600 text-white py-4 px-6 rounded-xl hover:from-gray-700 hover:to-slate-700 focus:ring-4 focus:ring-gray-200 font-semibold transition-all duration-200 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"></path>
            </svg>
            <span>Download Manifest</span>
          </button>
        </div>
      </div>

      <!-- Image Preview Section -->
      <div class="space-y-4">
        <h3 class="text-lg font-semibold text-gray-800">Image Preview</h3>
        <div id="imagePreview" class="grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-4">
          <!-- Images will be populated here -->
        </div>
      </div>

      <!-- New Job Button -->
      <button 
        id="newJobButton" 
        class="w-full mt-8 bg-gradient-to-r from-blue-600 to-indigo-600 text-white py-4 px-6 rounded-xl hover:from-blue-700 hover:to-indigo-700 focus:ring-4 focus:ring-blue-200 font-semibold transition-all duration-200 transform hover:scale-[1.02] shadow-lg hover:shadow-xl"
      >
        <div class="flex items-center justify-center space-x-3">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
          </svg>
          <span>Start New Scraping Job</span>
        </div>
      </button>
    </div>

    <!-- Features Section -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mt-16">
      <div class="text-center p-6">
        <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
          </svg>
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">Lightning Fast</h3>
        <p class="text-gray-600">Advanced crawling algorithms ensure quick and efficient image extraction</p>
      </div>
      
      <div class="text-center p-6">
        <div class="w-16 h-16 bg-green-100 rounded-2xl flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"></path>
          </svg>
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">Respectful Scraping</h3>
        <p class="text-gray-600">Follows robots.txt and implements proper delays to be respectful to servers</p>
      </div>
      
      <div class="text-center p-6">
        <div class="w-16 h-16 bg-purple-100 rounded-2xl flex items-center justify-center mx-auto mb-4">
          <svg class="w-8 h-8 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
        </div>
        <h3 class="text-xl font-semibold text-gray-900 mb-2">High Quality</h3>
        <p class="text-gray-600">Extracts images in their original quality and format</p>
      </div>
    </div>

    <!-- Footer -->
    <div class="text-center mt-16 pt-8 border-t border-gray-200">
      <p class="text-sm text-gray-500 mb-2">
        <span class="font-medium">Important:</span> Respect robots.txt. Only scrape content you have rights to use.
      </p>
      <p class="text-xs text-gray-400">
        Built with ❤️ using Rails 8 and modern web technologies
      </p>
    </div>
  </div>

  <script>
    let currentJobId = null;
    let pollInterval = null;

    document.getElementById('scrapeForm').addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(e.target);
      const data = {
        job: {
          url: formData.get('url'),
          render_js: formData.has('render_js'),
          scrape_mode: formData.get('scrape_mode')
        }
      };

      const submitButton = document.getElementById('submitButton');
      submitButton.disabled = true;
      submitButton.innerHTML = `
        <div class="flex items-center justify-center space-x-3">
          <svg class="animate-spin w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"></path>
          </svg>
          <span>Creating Job...</span>
        </div>
      `;

      try {
        const response = await fetch('/jobs', {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'X-CSRF-Token': document.querySelector('[name="csrf-token"]').content
          },
          body: JSON.stringify(data)
        });

        const result = await response.json();

        if (response.ok) {
          currentJobId = result.job_id;
          showStatusCard();
          startPolling();
        } else {
          showError('Error: ' + (result.errors || ['Unknown error']).join(', '));
        }
      } catch (error) {
        showError('Network error: ' + error.message);
      } finally {
        submitButton.disabled = false;
        submitButton.innerHTML = `
          <div class="flex items-center justify-center space-x-3">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
            </svg>
            <span>Start Scraping Images</span>
          </div>
        `;
      }
    });

    function showError(message) {
      // Create a beautiful error notification
      const notification = document.createElement('div');
      notification.className = 'fixed top-4 right-4 bg-red-500 text-white px-6 py-4 rounded-xl shadow-lg z-50 transform transition-all duration-300 translate-x-full';
      notification.innerHTML = `
        <div class="flex items-center space-x-3">
          <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
          </svg>
          <span>${message}</span>
        </div>
      `;
      
      document.body.appendChild(notification);
      
      // Animate in
      setTimeout(() => notification.classList.remove('translate-x-full'), 100);
      
      // Remove after 5 seconds
      setTimeout(() => {
        notification.classList.add('translate-x-full');
        setTimeout(() => document.body.removeChild(notification), 300);
      }, 5000);
    }

    function showStatusCard() {
      document.getElementById('statusCard').classList.remove('hidden');
      document.getElementById('resultsCard').classList.add('hidden');
      document.getElementById('scrapeForm').scrollIntoView({ behavior: 'smooth' });
    }

    function showResultsCard() {
      console.log('Job completed successfully! Showing results card and starting automatic download...');
      document.getElementById('statusCard').classList.add('hidden');
      document.getElementById('resultsCard').classList.remove('hidden');
      
      // Set up download handlers
      document.getElementById('downloadZip').onclick = () => {
        console.log('Manual ZIP download clicked for job:', currentJobId);
        window.location.href = `/jobs/${currentJobId}/download.zip`;
      };
      
      document.getElementById('downloadManifest').onclick = () => {
        console.log('Manifest download clicked for job:', currentJobId);
        window.location.href = `/jobs/${currentJobId}/manifest.json`;
      };
      
      // Automatic ZIP download after 2 seconds
      setTimeout(() => {
        console.log('Starting automatic ZIP download for job:', currentJobId);
        window.location.href = `/jobs/${currentJobId}/download.zip`;
      }, 2000);
    }

    function startPolling() {
      if (pollInterval) clearInterval(pollInterval);
      
      pollInterval = setInterval(async () => {
        try {
          const response = await fetch(`/jobs/${currentJobId}`);
          const data = await response.json();
          
          console.log('Polling update:', data);
          updateStatus(data);
          
          if (data.status === 'done' || data.status === 'error') {
            clearInterval(pollInterval);
            if (data.status === 'done') {
              console.log('Job completed! Images found:', data.images_found);
              showResultsCard();
            } else {
              console.error('Job failed:', data.message);
              showError('Scraping failed: ' + (data.message || 'Unknown error'));
            }
          }
        } catch (error) {
          console.error('Polling error:', error);
        }
      }, 1500);
    }

    function updateStatus(data) {
      const statusText = document.getElementById('statusText');
      const progressText = document.getElementById('progressText');
      const progressBar = document.getElementById('progressBar');
      const statusMessage = document.getElementById('statusMessage');
      const pageCount = document.getElementById('pageCount');
      const imageCount = document.getElementById('imageCount');

      const statusLabels = {
        'queued': 'Queued',
        'running': 'Crawling',
        'done': 'Complete',
        'error': 'Error'
      };

      statusText.textContent = statusLabels[data.status] || data.status;
      progressText.textContent = `${data.progress}%`;
      progressBar.style.width = `${data.progress}%`;
      statusMessage.textContent = data.message || '';
      pageCount.textContent = data.pages_crawled || 0;
      imageCount.textContent = data.images_found || 0;

      if (data.status === 'error') {
        progressBar.className = 'bg-red-500 h-3 rounded-full transition-all duration-500 ease-out';
      }
    }

    document.getElementById('newJobButton').addEventListener('click', () => {
      currentJobId = null;
      if (pollInterval) clearInterval(pollInterval);
      document.getElementById('statusCard').classList.add('hidden');
      document.getElementById('resultsCard').classList.add('hidden');
      document.getElementById('scrapeForm').reset();
      document.getElementById('render_js').checked = true;
      document.getElementById('scrape_mode_current').checked = true;
      
      // Scroll to top
      window.scrollTo({ top: 0, behavior: 'smooth' });
    });

    // Add smooth scrolling for all internal links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
          target.scrollIntoView({ behavior: 'smooth' });
        }
      });
    });
  </script>
</div>